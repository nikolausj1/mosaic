// Sources/Engine/ is pure Foundation - no SwiftUI imports, ever (PRD §8).
// The layout tree, clamping, snapping, and export math land here in Phase 1,
// testable via the Build Guide's swiftc smoke-test recipe without an Xcode build.
import Foundation

enum Engine {
    static let version = 0
}
